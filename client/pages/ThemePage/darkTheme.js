import { COLOR } from "../../constant/color";

const DarkTheme = {
  isDark: true,
  colors: {
    backgroundColor: COLOR.backgroundColor,
    fontColor: COLOR.textPrimaryColor,
    btnBackgroundColor: COLOR.btnBackgroundColor,
  },
};
export default DarkTheme;
